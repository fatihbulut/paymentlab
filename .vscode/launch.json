{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ISO parser (main.go)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/parser",
            "cwd": "${workspaceFolder}",
            "env": {
                "GIN_MODE": "debug",
                "PORT": "8080"
            }
        }
    ]
}